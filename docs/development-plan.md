# План разработки: Генератор визуальных паттернов для FL-Studio

## Фаза 0: Концепт (1 день)

### Задачи
- [ ] Создать структуру проекта (без фреймворков):
  ```
  project/
  ├── index.html
  ├── style.css
  └── script.js
  ```
- [ ] Реализовать сухой HTML/CSS/JS прототип:
  - Статичная сетка 64×12 (простые div-элементы)
  - Выпадающий список для тональности
  - Ползунки для длины и BPM
  - Кнопка "Сгенерировать"
  - Простой вывод результата (текстом или цветными квадратами)
- [ ] Протестировать базовую логику генерации в браузере

### Критерии готовности
- Сайт открывается в браузере без сервера
- Генерация паттерна работает (даже если просто выводит массив чисел)
- Никаких анимаций, только функционал

---

## Фаза 1: Подготовка (1 день)

### Задачи
- [ ] Инициализация проекта (npm init / pnpm init)
- [ ] Установка зависимостей:
  - `midi-file` или `@tonejs/midi` для MIDI экспорта
  - `jest` для тестов
  - `typescript` и настройка tsconfig.json
- [ ] Создание структуры папок:
  ```
  src/
  ├── core/          # Движок генерации
  ├── ui/            # UI компоненты
  ├── utils/         # Утилиты и валидация
  ├── models/        # Модели данных
  └── index.ts       # Точка входа
  ```

### Критерии готовности
- Проект собирается без ошибок
- Базовые тесты проходят

---

## Фаза 1: Подготовка (1 день)

### Задачи
- [ ] Инициализация проекта (npm init / pnpm init)
- [ ] Установка зависимостей:
  - `midi-file` или `@tonejs/midi` для MIDI экспорта
  - `jest` для тестов
  - `typescript` и настройка tsconfig.json
- [ ] Создание структуры папок:
  ```
  src/
  ├── core/          # Движок генерации
  ├── ui/            # UI компоненты
  ├── utils/         # Утилиты и валидация
  ├── models/        # Модели данных
  └── index.ts       # Точка входа
  ```

### Критерии готовности
- Проект собирается без ошибок
- Базовые тесты проходят

---

## Фаза 2: Модели данных и валидация (2 дня)

### Задачи
- [ ] Создать интерфейсы моделей (models/):
  - `Pattern`, `Note`, `Tone`, `GenerationOptions`
  - `ProjectFile`, `Preset`
- [ ] Реализовать валидатор (utils/validator.ts):
  - `validateBPM(bpm: number)`
  - `validateLength(length: number)`
  - `validateTone(tone: Tone)`
- [ ] Написать unit-тесты для валидатора

### Критерии готовности
- Все валидации работают корректно
- Тесты покрывают граничные случаи

---

## Фаза 3: Движок генерации (3 дня)

### Задачи
- [ ] Создать генератор гаммы (core/scale.ts):
  - `MAJOR_SCALE_INTERVALS`, `MINOR_SCALE_INTERVALS`
  - `getScale(root, tone)` — получение нот гаммы
- [ ] Реализовать алгоритм генерации нот (core/generator.ts):
  - `generateNotes(scale, length, options)`
  - Избегание диссонирующих интервалов
- [ ] Реализовать правила гармонии:
  - `isConsonant(interval)` — проверка консонантности
  - `getNextNote(currentNote, scale, options)` — выбор следующей ноты
- [ ] Создать основной генератор (core/patternGenerator.ts):
  - `generate(options: GenerationOptions): Pattern`
  - `generateFromPreset(preset: Preset): Pattern`

### Критерии готовности
- Генерация паттерна занимает <500ms
- Все ноты из выбранной гаммы
- Нет диссонирующих интервалов

---

## Фаза 4: UI компоненты (4 дня)

### Задачи
- [ ] Создать PatternEditor (ui/patternEditor.ts):
  - Сеточное представление (64×12)
  - Рендеринг через Canvas API
  - Цветовая кодировка нот
- [ ] Создать SettingsPanel (ui/settingsPanel.ts):
  - Выбор тональности (select)
  - Слайдер длины (1-64)
  - Слайдер BPM (40-240)
  - Кнопка генерации
- [ ] Создать PresetBrowser (ui/presetBrowser.ts):
  - Список пресетов
  - Загрузка/сохранение
- [ ] Интеграция UI с движком

### Критерии готовности
- UI отображает паттерн корректно
- Настройки влияют на генерацию
- Интерфейс отзывчив

---

## Фаза 5: Экспорт и сохранение (2 дня)

### Задачи
- [ ] Реализовать MIDI экспорт (utils/midiExporter.ts):
  - `patternToMidi(pattern: Pattern)`
  - Экспорт в файл
- [ ] Реализовать сохранение в .fpp (utils/fileIO.ts):
  - `savePattern(pattern, path)`
  - `loadPattern(path)`
- [ ] Добавить обработку ошибок

### Критерии готовности
- MIDI файл создается корректно
- Паттерны сохраняются и загружаются

---

## Фаза 6: Пресеты (2 дня)

### Задачи
- [ ] Создать 5 базовых пресетов (presets/):
  - Simple Major (16 шагов, 120 BPM)
  - Minor Groove (16 шагов, 100 BPM)
  - Fast Arpeggio (32 шага, 160 BPM)
  - Slow Chords (8 шагов, 80 BPM)
  - Complex Pattern (64 шага, 140 BPM)
- [ ] Реализовать сохранение пользовательских пресетов
- [ ] Реализовать экспорт/импорт пресетов

### Критерии готовности
- Пресеты загружаются корректно
- Пользовательские пресеты сохраняются

---

## Фаза 7: Финальная проверка (1 день)

### Задачи
- [ ] Покрытие всех требований из ТЗ
- [ ] Unit-тесты для всех компонентов
- [ ] Проверка производительности
- [ ] Исправление багов

### Критерии готовности
- Все тесты проходят
- Генерация <500ms
- Интерфейс отзывчив

---

## Итоговый срок: **14 дней**

### Оптимизация (MVP за 9 дней)
Если нужно ускорить:
- Концепт: упростить до текстового вывода (0.5 дня)
- Отложить круговую диаграмму
- Оставить 2-3 пресета вместо 5
- Убрать интерактивное редактирование (только генерация)
- Unit-тесты вместо property-based
